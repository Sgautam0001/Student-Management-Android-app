buildscript {
    repositories {
        google()
        mavenCentral()
        jcenter() // ok to keep for old libs (but deprecated)
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.1.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        jcenter() // ok to keep for old libs (but deprecated)
    }
}
